#include <iostream>
#include <conio.h>
#include <math.h>

void nhap(float &x, int &n)
{
    printf ("Nhap so nguyen n = ");
    scanf ("%d", &n);
    printf ("Nhap so thuc x = ");
    scanf ("%f", &x);
}

float tinh_F(float x, int n)
{
    float F = 2000.0 * x;
    for (int i = 3; i <= n; i++)
    
    {
        
    }
}